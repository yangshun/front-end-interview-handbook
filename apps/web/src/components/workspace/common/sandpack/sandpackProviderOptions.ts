const sandpackProviderOptions: Readonly<{
  bundlerURL: string;
}> = {
  // Default options for SandpackProvider.
  bundlerURL: 'https://bundler.gfe.zone',
};

export default sandpackProviderOptions;
