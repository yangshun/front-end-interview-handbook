---
title: 唯一数组
excerpt: 实现一个函数，从数组中删除所有重复的值
---

实现一个函数 `uniqueArray`，它接受一个数组并返回该数组的无重复版本，其中仅保留每个元素的第一次出现。结果值的顺序由它们在数组中出现的顺序决定。

## 例子

```js
uniqueArray([1, 2, 3]); // [1, 2, 3]
uniqueArray([1, 1, 2]); // [1, 2]
uniqueArray([2, 1, 2]); // [2, 1]
```
