import MDXCodeBlock from 'MDXCodeBlock';

import useEventListener from '../setup/src/use-event-listener';

## Solution

The `useEventListener` hook can be implemented with `useEffect` to attach and detach event listeners to the target element with `addEventListener` and `removeEventListener`, respectively.

<MDXCodeBlock>{useEventListener}</MDXCodeBlock>
