import MDXCodeBlock from 'MDXCodeBlock';

import fromPairsJs from '../setup/src/from-pairs.js';
import fromPairsTs from '../setup/src/from-pairs.ts';
import fromPairsFromEntries from '../setup/src/from-pairs-from-entries';

## Solution

### Approach 1: Loop through the `pairs` array

A for-of loop can be used to iterate through `pairs` and set the key and value on a `result` object.

<MDXCodeBlock languages={{ jsx: fromPairsJs, tsx: fromPairsTs }} />

### Approach 2: Use `Object.fromEntries()`

There's a built-in static method on `Object` to do exactly this -- it transforms a list of key-value pairs into an object.

<MDXCodeBlock>{fromPairsFromEntries}</MDXCodeBlock>

## Resources

- [Lodash `_.fromPairs`](https://lodash.com/docs/#fromPairs)
- [Object.fromEntries() - JavaScript | MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/fromEntries)
