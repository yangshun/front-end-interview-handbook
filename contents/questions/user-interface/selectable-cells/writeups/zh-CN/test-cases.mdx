## 测试用例

* 渲染一个 10 x 10 的网格。
* 绘制一个选择框
  * 在鼠标按下时绘制一个框，该框的位置和尺寸跟随拖动原点和鼠标的当前位置。
  * 在拖动原点周围移动光标，该框应正确渲染。
  * 释放鼠标后，选择框消失。
  * 从页面上的各个位置开始绘制：网格外部、网格内部等。
* 在网格中选择单元格
  * 在网格上绘制框时，任何重叠的单元格都应高亮显示。
  * 在屏幕上的任何位置单击都会导致高亮显示的单元格不再高亮显示。
