## Notes

- Besides the `mouseup` event, users will also expect other events like `contextmenu`, `mouseleave`, `touchend` to end the dragging. However in an interview setting, these events are probably acceptable to omit.
