## 笔记

* `overflow: hidden` 必须添加到 `.progress-bar`，因为有圆角，这样填充的进度就不会超出圆角。
* 应该妥善处理超出 \[0, 100] 范围的进度值；它们不应导致布局问题。
* 对于小值，可能没有足够的空间来显示百分比标签。 我们可以不显示任何内容或截断显示。
* 我们可以使用 CSS 转换（例如 `scale`）而不是更改 `width` 属性，如果需要动画，这对于性能更好。

## 可访问性

* 为了 a11y 的原因，将 `role="progressbar"` 和 `aria` 值添加到组件中，以便屏幕阅读器可以准确地描述该组件。
