import TestCases from '../../../test-cases.mdx';
import Notes from '../../../notes.mdx';

## 解决方案

使用 React 元素的 `style` 属性可以按比例填充进度条（0-100 之间的数字，包括 0 和 100）。由于该值是动态的，我们不可能事先为其编写类，因此我们必须使用内联样式。

<TestCases />

<Notes />
