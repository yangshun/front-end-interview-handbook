import TestCases from '../../../test-cases.mdx';

## 解决方案

这是一个简短的问题，只需要一个状态变量，即按钮被点击的次数 (`count`)。

将 `count` 声明为实例变量，并创建一个 `increment` 方法来递增 `count`。此方法绑定到 `<button>` 的点击操作，并在单击 `<button>` 时调用。

<TestCases />
