import TestCases from '../../../test-cases.mdx';

这是一个简短的问题，只需要一个状态变量，即按钮被点击的次数（`count`）。将一个`'click'`事件监听器附加到`<button>`，每次点击时，增加`count`值，并使用该值更新文本。

由于只需要更改计数，我们可以使用一个`<span>`来定位计数文本，并在每次触发点击事件时更新其`textContent`。

<TestCases />
