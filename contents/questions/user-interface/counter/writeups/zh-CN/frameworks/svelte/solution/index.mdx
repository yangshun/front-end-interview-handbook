import TestCases from '../../../test-cases.mdx';

这是一个简短的问题，只需要一个状态变量，即按钮被点击的次数 (`count`)。

将 `count` 声明为一个变量。在 `<button>` 的 `on:click` 处理器上附加一个回调，并在其中递增 `count` 值。 只要点击 `<button>`，`count` 就会递增。

<TestCases />
