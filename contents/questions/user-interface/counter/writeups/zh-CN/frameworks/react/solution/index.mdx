import TestCases from '../../../test-cases.mdx';

这是一个简短的问题，只需要一个状态变量，即按钮被点击的次数 (`count`)。

`useState` 可用于跟踪该变量，并将硬编码的 0 替换为 `count` 状态。 然后在 `<button>` 元素上附加一个 `onClick` 处理程序，并在每次单击时递增 `count`。

<TestCases />
