---
title: Data Table III
excerpt: Build a generalized data table with pagination and sorting features
---

**Note:** This is a follow up to the [Data Table II](/questions/user-interface/data-table-ii) question, you should complete that question first before attempting this question.

In [Data Table II](/questions/user-interface/data-table-ii), we built a users data table that displays users in a paginated format and allows for sorting rows by specific columns in both ascending and descending order.

However, the `DataTable` component is tightly coupled to the user data it is meant to be rendered and cannot be used for other types of data.

Generalize the component to take in the data and any necessary configuration so that the `DataTable` component can render any kinds of data.

## Requirements

- The `DataTable` component should not contain any logic that is specific to users.
- Modify the `DataTable` component to take in any props/attributes necessary to be generalized.
- The appearance and functionality of the end result should remain – it should look the same and still allow for pagination and sorting.

To assist in your testing, another data set (`src/data/houses.json`) is being provided for you to test your generalized component with.

**Hint**: Have a look at [TanStack Table](https://tanstack.com/table/) to get a sense of the configuration approach and available options for production-ready data table libraries.

## Notes

The skeleton code uses the solution of [Data Table II](/questions/user-interface/data-table-ii), but you are free to use your own solution as a starting point.
