## Notes

* 为表单字段使用`<label>`来指示输入内容。
* 使用`<input type="number">`有助于仅允许数值和步进控件。
* 数字格式化很棘手：
  * `Number()`构造函数：将值转换为数字，如果不可能则转换为`NaN`。请注意，这会返回`Number(anyFalseyValue)`，得到0，因此我们需要区分空字符串的情况和真实的0。
  * `Number.IsNaN()`：确定一个值是否是数字。
  * `/\.\d{5}/`：此正则表达式测试一个数字是否具有5位或更多小数位。
