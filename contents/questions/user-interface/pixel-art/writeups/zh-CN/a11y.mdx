## 可访问性

* 对交互元素（例如“绘制”/“擦除”按钮和颜色选择器选项）使用`<button>`。
* 为颜色选项按钮添加`aria-label`，因为它们没有可见标签。
* 由于每个画布单元格都是一个`<button>`，您可以通过<kbd>Tab</kbd>键在它们之间切换，并使用<kbd>Space</kbd>键触发它们上当前选定的操作。对于额外加分，您可以添加键盘导航（上、下、左、右）以轻松浏览单元格，使用<kbd>Enter</kbd>键绘制，使用<kbd>Backspace</kbd>键擦除。
