## Accessibility

- Since the `<input>` fields don't have visible labels, `aria-label`s can be used to label the fields for screenreaders.
- Wrapping the fields in a `<form>` enables default form submission behavior.
