## 可访问性

虽然可访问性不是这个问题的重点，但我们可以做一些事情来改善这个应用程序的可访问性和用户体验。

* 使用`<button>`渲染网格单元格，以便可以通过<kbd>Tab</kbd>进行聚焦，并通过<kbd>Space</kbd>和<kbd>Enter</kbd>进行激活。 这样，我们就可以完全使用键盘激活网格。
* 所有按钮都应该有标签，因此我们添加带有索引的`aria-label`来描述单元格。 通过指出列号和行号，可以进一步改进标签。
* 禁用已激活的单元格和取消激活时的按钮也很有帮助，这样单元格就不能再被聚焦或响应点击。
