## 解决方案

我们将在[文件资源管理器的解决方案](/questions/user-interface/file-explorer/react/solution)的基础上进行构建。

### 返回片段

主要的变化是返回片段而不是`<ul>`。根`<ul>`由`FileExplorer`组件渲染，`FileObject`和`FileList`应该返回片段，并作为`FileExplorer`中`<ul>`的直接子级渲染。

### 渲染缩进

每个缩进级别要使用的左侧填充量可以通过`level`属性确定 - 将级别（首先减去一）乘以缩进宽度。
