## 测试用例

* "Default" 状态
  * 悬停按钮应显示 "Hovered" 状态。
  * 点击按钮应显示加载微调器。
    * 如果请求成功，按钮应转换为 "Liked" 状态。
    * 如果请求失败，按钮应返回到 "Default" 状态，并在按钮下方显示错误消息。
* "Liked" 状态
  * 在此状态下悬停按钮是未定义的行为。悬停时未显示任何差异是可以的。
  * 点击按钮应显示加载微调器。
    * 如果请求成功，按钮应转换为 "Default" 状态。
    * 如果请求失败，按钮应返回到 "Liked" 状态，并在按钮下方显示错误消息。
