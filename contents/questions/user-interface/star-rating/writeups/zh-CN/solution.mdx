## 解决方案

与 Vanilla 版本相比，使用框架/库的解决方案要短得多。

我们可以创建一个 `Star` 组件，该组件接受一个 `filled` 属性，该属性有条件地呈现适当的类名。

该组件将需要一个状态 `hoveredIndex`，用于跟踪当前悬停的星星的索引。

为了确定是否应该突出显示/填充 `Star`，我们检查当前是否有任何星星被悬停，如果是，则优先考虑，并且任何 `index <= hoveredIndex` 的星星都应该被突出显示。 否则，任何 `index < value` 的星星都可以被突出显示/填充。
