## 后续

一个可能出现问题的场景是同时打开多个对话框/堆叠，因为打开的对话框的内容允许打开另一个对话框。

我们当前实现的一个限制被暴露出来——由于大多数事件监听器都是在全局级别添加的，所有打开的对话框都可能响应<kbd>Escape</kbd>事件并同时关闭，而预期的行为是只关闭最顶层的对话框。

修复这个问题将需要代码来跟踪打开的对话框堆栈，并且只关闭最顶层的对话框。然而，实现这一点并非易事，通常超出了面试的范围。也就是说，向面试官提及这种情况可能会给你加分！
