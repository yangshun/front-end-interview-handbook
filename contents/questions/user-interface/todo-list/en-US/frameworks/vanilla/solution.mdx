## Solution

There are a few ways to approach this questions:

- Add only the necessary DOM event listeners and DOM operations. See [solution](/questions/user-interface/todo-list/vanilla/solution).
- Use `<template>`s and client-side render both initial and new tasks. See [solution](/questions/user-interface/todo-list/vanilla/solution/template).
