## 测试用例

* 骰子数量表单
  * 不允许无效值（空、负数、小数、大于 12）。
  * 按下“Roll”按钮会使用正确的值调用随机生成器函数。
  * 在 `<input>` 字段中按 <kbd>Enter</kbd> 即可提交表单。
* 骰子生成
  * 生成的值介于 1 到 6 之间（包括 1 和 6）。确保不可能生成 0。
  * 生成正确数量的值。
* 显示
  * 每行最多应包含 3 个骰子。
  * 如果有 3 个以上的骰子，则将其他骰子换行到下一行。
  * 每个骰子的点位对于每一面都是正确的。
* 辅助功能
  * 骰子滚动结果会播报给屏幕阅读器。
