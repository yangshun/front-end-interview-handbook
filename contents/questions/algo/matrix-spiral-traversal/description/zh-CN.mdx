---
title: 矩阵螺旋遍历
excerpt: 实现一个函数，以螺旋顺序遍历矩阵
---

import MDXTestExamples from 'MDXTestExamples';
import runTestCases from '../setup/src/run.tests.json';

给定一个大小为 `m` x `n` 的 `matrix`，编写一个函数以螺旋顺序返回矩阵的元素。

### 输入

- `matrix: number[][]`: 大小为 `m` x `n` 的二维数组 `matrix`

<MDXTestExamples testCases={runTestCases} />

### 约束

- 1 &lt;= `m`, `n` &lt;= 100
- -1,000 &lt;= `matrix[row][col]` &lt;= 1,000
