---
title: 查找序列中缺失的数字
excerpt: 实现一个函数来查找已排序数组中缺失的元素
---

import MDXTestExamples from 'MDXTestExamples';
import runTestCases from '../setup/src/run.tests.json';

给定一个大小为 `n` 的数组 `numbers`，用于存储 `n` 个不同的整数，这些整数的范围在 `[0, n]` 之间，实现一个函数来识别数组中缺失的元素。除了一个数字外，所有数字都存在于数组中。找到缺失的数字。

### 输入

- `numbers: number[]`: 整数数组

<MDXTestExamples testCases={runTestCases} />

### 约束

- 1 &lt;= `n` &lt;= 10,000
- 0 &lt;= `numbers[i]` &lt;= `n`
