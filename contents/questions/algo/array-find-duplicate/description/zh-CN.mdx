---
title: 查找数组中的重复项
excerpt: 实现一个函数来检查数组中是否有任何重复的数字
---

import MDXTestExamples from 'MDXTestExamples';
import runTestCases from '../setup/src/run.tests.json';

给定一个整数数组 `numbers`，确定该数组是否包含任何重复值。重复定义为数组中出现多次的任何数字。

### 输入

- `numbers: number[]`: 整数数组

<MDXTestExamples testCases={runTestCases} />

### 约束

- 1 &lt;= `numbers.length` &lt;= 10,000
- -1,000,000 &lt;= `numbers[i]` &lt;= 1,000,000
