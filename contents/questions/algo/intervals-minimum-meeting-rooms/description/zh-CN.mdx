---
title: 所需最少会议室
excerpt: 实现一个函数来查找所需的最少会议室数量
---

import MDXTestExamples from 'MDXTestExamples';
import runTestCases from '../setup/src/run.tests.json';

给定一个会议时间 `intervals` 数组，其中每个间隔表示为 `[start, end]`，表示会议在 `start` 时间开始，在 `end` 时间结束，确定容纳所有会议所需的最少会议室数量。

### 输入

- `intervals: Array<[number, number]>`: 整数对数组

<MDXTestExamples testCases={runTestCases} />

### 约束

- 0 &lt;= `intervals.length` &lt;= 1000
- `intervals[i].length` == 2
- 0 &lt;= `start` \< `end` &lt;= 1,000,000
