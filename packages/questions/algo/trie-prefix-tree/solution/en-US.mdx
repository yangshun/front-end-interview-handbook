import MDXCodeBlock from 'MDXCodeBlock';

import trie from '../setup/src/trie-prefix-tree.ts';

## 1. Using Trie data structure

Time complexity: O(n.m), where `n` is the length of `methods` and `m` is the average length of each word in words array

<MDXCodeBlock>{trie}</MDXCodeBlock>
