import MDXCodeBlock from 'MDXCodeBlock';

import differenceJs from '../setup/src/difference.js';
import differenceTs from '../setup/src/difference.ts';
import differenceFilter from '../setup/src/difference-filter';
import differenceFilterSet from '../setup/src/difference-filter-set';

## Solution

<MDXCodeBlock languages={{ jsx: differenceJs, tsx: differenceTs }} />

Here's a simpler solution that leverages `Array.prototype.filter` and checking of the set.

<MDXCodeBlock>{differenceFilterSet}</MDXCodeBlock>

The set can also be omitted by using `Array.prototype.includes` instead.

<MDXCodeBlock>{differenceFilter}</MDXCodeBlock>

## Edge cases

To handle sparse arrays such as `[1, ,3]`, the `in` operator is used check if the index `i` is present in the `array` before checking if the value at index `i` is undefined. This ensures that sparse arrays are handled correctly.

## Resources

- [Lodash `_.difference`](https://lodash.com/docs/#difference)
