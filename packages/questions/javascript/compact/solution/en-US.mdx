import MDXCodeBlock from 'MDXCodeBlock';

import compact from '../src/compact';

## Solution

The solution is to make use the conditional evaluation of Javascript.

<MDXCodeBlock>{compact}</MDXCodeBlock>

## Edge Cases

Objects such as `{}` are evaluated to `true`.

## Resources

- [Lodash `_.compact`](https://lodash.com/docs/#compact)
