import MDXCodeBlock from 'MDXCodeBlock';

import inRange from '../src/in-range';
import inRangeShort from '../src/in-range-short';

## Solution

<MDXCodeBlock>{inRange}</MDXCodeBlock>

To check for if the `end` parameter is being passed into the function, use `end === undefined`.

Here's a shorter solution which is more concise but a little less obvious.

<MDXCodeBlock>{inRangeShort}</MDXCodeBlock>

## Resources

- [Lodash `_.inRange`](https://lodash.com/docs/#inRange)
