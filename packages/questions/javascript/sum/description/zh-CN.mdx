---
title: 求和
excerpt: 实现一个函数，该函数通过接受一个数字进行求和，并允许重复调用，传入更多数字，直到不传入任何数字为止。
---

实现一个 `sum` 函数，该函数接受一个数字并允许重复调用，传入更多数字。不带参数调用该函数将对到目前为止的所有参数求和并返回总和。

## 例子

```js
sum(1)(); // 1
sum(1)(2)(); // 3
sum(1)(2)(-3)(); // 0
```
