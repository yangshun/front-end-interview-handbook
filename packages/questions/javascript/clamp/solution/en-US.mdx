import MDXCodeBlock from 'MDXCodeBlock';

import clampJs from '../setup/src/clamp.ts';
import clampTs from '../setup/src/clamp.ts';

## Solution

<MDXCodeBlock languages={{ jsx: clampJs, tsx: clampTs }} />

## Edge Cases

- `upper` is smaller than `lower`. We can assume we aren't given this case.

## Resources

- [Lodash `_.clamp`](https://lodash.com/docs/#clamp)
