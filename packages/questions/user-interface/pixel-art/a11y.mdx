## A11y

Not implemented in solution, but for extra credit, you can add keyboard navigation events (up, down, left, right) to select cells and draw/erase with Enter.

Consider using a button instead of a div for semantic HTML.
