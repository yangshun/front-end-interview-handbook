## Test cases

TODO(react)

- The board should render a 15x15 grid.
- Board should not have any colors applied on initial load.
- There should be a ColorPicker that shows which color is currently selected.
- Clicking once on a Cell while Drawing should color that cell.
- Clicking and dragging over multiple cells while Drawing should color those cells.
- Clicking once on a Cell while Erasing should erase that cell's color.
- Clicking and dragging over multiple cells while Erasing should remove those cell's colors.
- Drawing over an already colored cell with a different color should update that color.
