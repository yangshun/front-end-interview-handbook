## A11y

- Use of `<form>` submit for pefroming actions.
- `<input type="text">` are paired with `<label>`s. Clicking on labels also focuses the input.
- All required functionality should be achievable just by using the keyboard.
