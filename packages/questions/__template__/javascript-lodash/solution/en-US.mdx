import MDXCodeBlock from 'MDXCodeBlock';

import todoReplaceMeJs from '../src/todo-replace-me.js';
import todoReplaceMeTs from '../src/todo-replace-me.ts';
import todoReplaceMeAltJs from '../src/todo-replace-me-alt.js';
import todoReplaceMeAltTs from '../src/todo-replace-me-alt.ts';

## Solution

There are two common ways to double a number.

### Solution 1: Multiplying by 2 (Remove heading if only one solution)

<MDXCodeBlock languages={{ jsx: todoReplaceMeJs, tsx: todoReplaceMeTs }} />

### Solution 2: Adding the number to itself (Remove heading if only one solution)

<MDXCodeBlock
  languages={{ jsx: todoReplaceMeAltJs, tsx: todoReplaceMeAltTs }}
/>

## Edge Cases

- TODO_REPLACE_ME

## Resources

- [Lodash `_.todoReplaceMe`](https://lodash.com/docs/#todoReplaceMe)
