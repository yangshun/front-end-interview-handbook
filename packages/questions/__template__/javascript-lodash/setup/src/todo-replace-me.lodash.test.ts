// import { todoReplaceMe } from 'lodash';

// The purpose of this file is to make sure Lodash's implementation can
// also pass our tests
// Keep this file in sync with the unit tests.
describe('todoReplaceMe', () => {
  test('foo', () => {
    expect(1).toBe(1);
  });
});
